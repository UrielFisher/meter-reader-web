services:
  backend:
    build: ./back
    volumes:
      - ./sqlite_db:/app/sqlite_db
    expose:
      - "5000"
    networks:
      - mr_net

  frontend:
    build: ./front
    expose:
      - "80"
    networks:
      - mr_net


networks:
  mr_net: